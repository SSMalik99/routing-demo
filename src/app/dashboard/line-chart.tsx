export default function LineChart() {
    return <>
    <div>
        This is line chart Page not visible because only exported function from the page.tsx is renderd after the 
        routing,  we should keep those things in component folder which is a good convention
    </div>
    </>
}